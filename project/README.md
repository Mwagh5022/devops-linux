Steps for Execute

pull this repo
then check the permission of add_user.sh

    ls -la
or 
  
    ls -l add_user.sh
it's should be 755, if not then run command

    chmod 755 add_user.sh
for Run .sh file

    ./add_user.sh
