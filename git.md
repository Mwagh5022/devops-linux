Configure Git (User Identity)

Before using Git, set your name and email:

git config --global user.name "Manish Wagh"
git config --global user.email "manish@example.com"


Verify your configuration:

git config --list
